export enum StoreNames {
    SCHEDULER = 'scheduler',
    CHATBOT = 'chatbot',
    USER = 'user',
    LINK = 'link',
    CONTACT_CARD = 'contact_card',
    ATTACHMENT = 'attachment',
    CSV = 'csv',
}
